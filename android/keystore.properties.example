# Example keystore.properties for Android release signing
# Copy this file to keystore.properties and update with your keystore credentials
# DO NOT commit keystore.properties or your .keystore/.jks file to source control!

storeFile=../my-release-key.jks
storePassword=your_keystore_password
keyAlias=your_key_alias
keyPassword=your_key_password
